export interface Urls {
    id: number,
    delivered_to: string
    long_url: string
    short_url: string,
    status: string,
    source: string
    clicks: number,
    code: string,
    createdAt: string,
    updatedAt: string
}